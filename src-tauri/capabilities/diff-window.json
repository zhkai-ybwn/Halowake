{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "diff-window-capability",
  "description": "Permissions for the standalone Git Diff window",
  "windows": ["git-diff"],
  "permissions": [
    "core:default",
    "core:event:default"
  ]
}
